# ===================================
# Main
# ===================================

# Add the names of the created app (must be in: ./app/)
# to include them in the work
apps = [
    # Apps:
    # Enter the name of your app

    # Default:
    'core',
]

# Don't forget to make edits in .gitignore
db_url = 'sqlite://database.sqlite3'

# ===================================
# template / static
# ===================================

template_path = 'templates'

static_path = 'static/'
static_url = '/static/'
